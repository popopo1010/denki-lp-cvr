# 独自予約システム（TimeRex不要）

LPの電話・名前を使い、**サンクスで日時だけ選ぶ** → **Googleカレンダーに予定作成** → **スプシ更新 + Slack通知**。

## 構成

```
サンクス（独自UI・3日表示）
  ↓ JSONP 空き枠取得
GAS + Googleカレンダー（担当者の空き）
  ↓ 予約 POST
Googleカレンダーにイベント作成 + スプシ + Slack
```

## 1. あなたがやること（初回）

**実行アカウント:** `yuki.shibayama@xchange-inc.com`（GASデプロイ・Webアプリ実行ともこのアカウント）

### Googleカレンダー（予約は自動であなたのカレンダーに入る）

LPで枠を選んで「予約する」と、**GASデプロイアカウントのGoogleカレンダー**に予定が1件作成されます（追加操作不要）。

1. GASエディタで **`getBookingCalendarInfo`** を実行 → ログの `calendar_id` をコピー
2. プロジェクトの設定 → **スクリプトプロパティ** → `BOOKING_CALENDAR_ID` = その ID（例: `yuki.shibayama@xchange-inc.com`）
3. 未設定の場合は **デプロイユーザー（yuki.shibayama@xchange-inc.com）のメインカレンダー** に入ります
4. 手動で ID を確認: Googleカレンダー → 対象カレンダー ⚙ → **カレンダーの統合** → **カレンダーID**
5. TimeRex用 `yuki.shibayama_34d4` とは別。独自予約の正本は **このGoogleカレンダー**

### GAS（コード反映済み @12）

```bash
cd gas-recorder
clasp push -f
clasp redeploy AKfycbzC4fMEbOhaymimRwaLDJ34eKwSRyfYVVRMeNGl_cMjR8p7dC9cVw84YZJUvggkROiKRw
```

### ★ カレンダー権限の承認（必須・1回だけ）

1. **`yuki.shibayama@xchange-inc.com` でログイン**したブラウザで GAS を開く:  
   https://script.google.com/home/projects/1PLdit_rSh-OaParVho8E-DhBFZz4lqSa7ZF6EFsLBzncs5uERinNFUl2/edit
2. 関数 **`testBookingSlots`** → **実行**（カレンダー権限の承認）
3. 関数 **`getBookingCalendarInfo`** → **実行**（使うカレンダーIDをログに表示・プロパティ用）
4. 「権限を確認」→ **yuki.shibayama@xchange-inc.com** で許可（Googleカレンダー）
5. 実行ログに `slots: （0より大きい数字）` → 完了

承認前はサンクスで「空き枠の取得に失敗」と表示されます。

### Slack（任意）

スクリプトプロパティ `SLACK_WEBHOOK_URL` を設定（TimeRex時と同じ）。

### サンクス（リポジトリ）

`assets/js/thanks-booking-config.js`:

```javascript
window.THANKS_BOOKING_MODE = "custom";  // 独自予約
```

TimeRexに戻す場合は `"timerex"` に変更。

## 2. 受付ルール（GASで自動）

| 項目 | 既定値 | プロパティで変更 |
|------|--------|------------------|
| 枠の長さ | 15分 | `BOOKING_SLOT_MINUTES` |
| 受付時間 | 平日 9:00〜18:00 | `BOOKING_START_HOUR` / `BOOKING_END_HOUR` |
| 何時間後から | 2時間後 | `BOOKING_LEAD_HOURS` |
| 何日先まで | 14日（サンクスAPIは5日） | `BOOKING_DAYS_AHEAD` |

空き判定は **そのカレンダーの既存予定と重ならない枠** のみ表示します。

### 表示速度（Cache + 静的JSON）

| 層 | 内容 |
|----|------|
| GAS `CacheService` | 空き枠レスポンスを **3分**キャッシュ（`booking-custom.js`） |
| 時間トリガー | `warmBookingSlotsCache` を **5分ごと**（推奨） |
| 静的JSON | `assets/data/booking-slots.json` … deploy / 5分CIで更新 |
| ブラウザ | 静的JSON → 失敗時GAS JSONP、sessionStorage 5分 |

**GAS反映（コード変更後）**

```bash
cd gas-recorder
clasp push -f
clasp redeploy AKfycbzC4fMEbOhaymimRwaLDJ34eKwSRyfYVVRMeNGl_cMjR8p7dC9cVw84YZJUvggkROiKRw
```

**トリガー追加（GASエディタ）**

- 関数: `warmBookingSlotsCache`
- 種類: 時間主導型 / 5分おき

**ローカルでJSON生成**

```bash
node scripts/sync-booking-slots.js
```

## 3. テスト

1. LP送信 → サンクス（`thanks/preview.html` でも可）
2. 3日分の時間ボタンが表示される
3. 時間を選び「この日時で予約する」
4. **Googleカレンダー（BOOKING_CALENDAR_ID で指定したカレンダー）**に予定が入る
5. 入らない場合: GASで `testBookSlot` を実行 → 同じカレンダーにテスト予定が入るか確認
5. Slackに日時が届く（Webhook設定時）

空き取得API（ブラウザ）:

```
https://script.google.com/macros/s/AKfycbz.../exec?action=slots&callback=test
```

## 4. TimeRexとの比較

| | 独自 | TimeRex |
|--|------|---------|
| 月額 | なし（GAS無料枠） | あり |
| 名前メール再入力 | なし | 削除不可 |
| 3日表示 | UIで制御 | 埋め込み依存 |
| 空きの元 | 自社Googleカレンダー | TimeRex設定 |

## 5. 担当者複数（空きマージ＋ラウンドロビン）— 方式A

求職者には **日時だけ** 表示。裏で柴山・林・福山・山田の空きを OR マージし、予約確定時に空いている担当へ **均等割当（RR）** します。

### スクリプトプロパティ `BOOKING_STAFF_JSON`

GAS → プロジェクトの設定 → スクリプトプロパティ → 新規:

| 名前 | 値（1行JSON・改行なし） |
|------|-------------------------|
| `BOOKING_STAFF_JSON` | 下記例をコピーし、各 `calendar_id` を実IDに差し替え |

```json
[{"id":"shibayama","name":"柴山","calendar_id":"yuki.shibayama@xchange-inc.com"},{"id":"hayashi","name":"林","calendar_id":"林のカレンダーID"},{"id":"fukuyama","name":"福山","calendar_id":"福山のカレンダーID"},{"id":"yamada","name":"山田","calendar_id":"山田のカレンダーID"}]
```

- `calendar_id` が空・無効な担当は **自動でスキップ**（柴山だけでも動作）
- 未設定のときは従来どおり `BOOKING_CALENDAR_ID` / デフォルトカレンダー1本

### カレンダーIDの調べ方

1. 各担当の Googleカレンダーを **GAS実行アカウント**（`yuki.shibayama@xchange-inc.com`）と共有（変更権限）
2. GASエディタで **`getBookingStaffInfo`** を実行 → ログに `staff[]` が出る
3. 反映後: **`clasp push`** → **再デプロイ**（既存デプロイID）→ **`warmBookingSlotsCache`** トリガー継続

### スプシ・Slack

- 列追加: `calendar_staff_id`, `calendar_staff_name`
- Slack: `*担当:* 林` を付与
- カレンダー件名: `【LP面談・林】山田 太郎 090...`

### 負荷

- ユーザー: JSON 1本のまま（枠はマージされるので **空きは増えやすい**）
- GAS: 担当数ぶん `getEvents`（5分キャッシュで平準化）

## 6. 今後の拡張（必要になったら）

- 管理画面で休日設定
- 予約確認メール（Gmail送信）
