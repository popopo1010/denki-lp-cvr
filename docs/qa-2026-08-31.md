# QA・全体最適化（2026-08-31）

本番反映済み 3件 / 本番未反映 1件 / 判断待ち 5件。

## 本番へ反映したもの

### 1. 求人カードの拡充とタップ導線（#124 / #125・denkikouji-trust のみ）

- データを 8件 → 36件（職種12種・8地域28都道府県）。並びは地域のラウンドロビンで、
  表示は先頭N件のスライスなので並び順がそのまま「全国に求人がある」の見え方になる。
- 年収は**相場+50万の訴求レンジ**（オーナー指示）。下限平均 428万 → 478万。
  相場の実測値ではないので `salary_basis` に根拠を残した。
- 表示 3件 → 6件。高さ予約 232 → 588px（実測585px・CLS 0）。
- **デッドタップの解消**：白背景＋枠線＋角丸で押せる見た目なのに何も起きなかった。
  カードを button 化し「詳細を見る ›」を添え、タップでFVの選択肢
  「求人情報を閲覧したい」を実際にクリックする。遷移・クマ移動・`form_step` 計測は
  既存ハンドラのまま（新しい経路を作らない）。委譲バインドなので描き直しに耐える。
- データは架空のままで `is_sample: true`。**`JobPosting` 構造化データは出さない**
  （架空求人の構造化データはスパム扱いになる）。JSONの `_note` にも明記。

### 2. 社名・運営会社リンクの是正（#125・全LP）

```
© XCHANGE / © XCHANGE Inc.      107箇所 → XCHANGE株式会社
運営会社 builders-job.com/company        → https://xchange-inc.com/
プライバシーポリシー3本 and-and.jp        → https://xchange-inc.com/
プライバシーポリシー3本 info@and-and.jp   → info@xchange-inc.com
```

`and-and.jp` は**XCHANGEとは別会社のドメイン**で、テンプレート流用元の値が残っていた。
個人情報の開示請求・問い合わせ窓口が他社アドレスになっていたため、表記ゆれより実害が大きい。
108ファイル・194箇所を置換し、旧ドメインの残存はゼロ。

### 3. Meta広告の着地7本の余白（#126）

全60本E2Eで発覚（953/967）。**今回の変更による退行ではなく以前からの不具合**。

原因は `assets/css/meta-short-lp.css:103` が入力ステップでヘッダーを `display:none`
にしていること。共通 critical CSS のアプリ内ブラウザ用の上余白 `64px` は
「先頭要素 ≒ 余白 + ヘッダー36px = 100px」を前提に決めた値（2026-08-29 に 96px から
下げた経緯）なので、ヘッダーを隠すと先頭要素が 64px に来る＝LINEのバー（実測83px）の
裏に沈む。**CLAUDE.md 頻出バグ⑤の再発が、一番広告費をかけている着地で起きていた。**

修正は「隠している側で36pxを埋め戻す」。共通CSSの 64px には触らない。

```css
html.dk-inapp.dk-ios body.meta-short-lp.lp-form-step .js-page-body { padding-top: 100px !important; }
```

反映後、全60本E2Eは **967/967**（953→967 に回復）。

## 本番未反映（判断待ち #1）

### FV画像(LCP)の二重ダウンロード（`f47e546`）

ページ実測でLPごとの転送量を出して発見。DPRを振って再現した：

```
修正前  DPR1  21KB（-sp のみ）
        DPR2  84KB（テーマ側960w 63KB ＋ -sp 21KB）
        DPR3  84KB（同上）
修正後  DPR1/2/3 すべて 21KB
```

iPhoneは全機種DPR2〜3、Androidも大半が2.6〜3なので、**実質すべてのスマホで
LCP画像を4倍の重さで取っていた**。

原因は、preload の候補選択が `imagesizes` に **DPRを掛けた実ピクセル幅**で行われること。
390px × DPR2 = 780px を要求するため 960w が選ばれる一方、`<picture>` は
`media="(max-width:767px)"` で -sp を選ぶ。両者が食い違って2枚落ちる。
PR #118 が別系統で直したのと同じ罠。

修正は preload を picture と同じ `media` で分けること（13ファイル）。あわせて
自前LP 10本にはモバイル用 -sp 画像自体が無かったので共有assetsから持ち込んだ
（220KB → 178KB / 272KB → 173KB）。

**記録**：最初は `imagesrcset` 方式で実装したが、実測すると 63+21=84KB と逆に重くなった。
食い違いを自分で作っていた。**measureしていなければ「最適化した」と報告して悪化させていた。**

## 調べ切って「やることがない」と確認した角度

```
使われないpreload          0件（全193HTML）
JSエンジンの重複読み込み    0件（app.js と app-v2.js を両方読むページなし）
大きい画像                全部webp併存・参照あり＝<picture>のフォールバック。消せない
未参照アセット             0件
privacypolicy 3本の内容    完全一致（差分はCSSパス1行のみ）
```

`theme-snapshot.css`（51KB・ほぼ全LPが読む）は最大の単一リソースだが**触らない**。
本番テーマとバイト同一に保つ規約があり、非同期化も 2026-08-19 に
「最低優先度フェッチになり低速回線でLCPが悪化する」として同期へ統一した経緯がある。

`app.js` / `app-v2.js`（各70KB）は `defer` 付きでレンダリングを止めない。削るには
フォーム本体に手を入れることになり、送信消失・自己修復・クマ移動という
このリポジトリで最も事故が多い領域に触れるため、数十KBに対してリスクが釣り合わない。

## 判断待ち

| # | 内容 | 待っているもの |
|---|---|---|
| 1 | FV画像修正のmainマージ | 許可（表示は不変・DPR2/3で 84→21KB） |
| 2 | WPLP 11本の追従CTA | A=正しく実装 / B=死んだHTMLを削除 / C=現状維持 |
| 3 | サーバに残った非Web資産の削除 | 案1=デプロイに後始末 / 案2=手で削除 / 案3=検証を外す |
| 4 | 「サービスについて」の工種別URL | 正しいURL（148箇所を差し替え） |
| 5 | 所在地・代表者の事実確認 | 確認（`and-and.jp` 汚染の残りの可能性） |

### #2 の詳細（WPLP 追従CTA）

`WPLP/assets/css/cvr-boost.css` に `.cvr-floating-cta__btn` の定義が無く、
あるのは `body.lp-form-step .cvr-floating-cta{display:none}` だけ。つまり
`position:fixed` も効かず、**素のボタン（210×18px）がページ末尾に置かれているだけ**で、
「画面下に追従する赤いCTA」は WPLP 11本では機能として存在していない。
正しい実装は `dk_lp/denkikouji/assets/css/cvr-boost.css:625-661`。

`docs/qa-2026-08-22.md:410` が「WPLPのHTMLが使うクラスのうち正本CSSに無いものが2つ
（`cvr-floating-cta__btn` / `__sub`）＝正本で上書きすると壊れる」としてCSS統合を
見送っていたが、**実際は両方に無い**。この判断の前提が誤っていた。

### #3 の詳細（非Web資産の公開）

`CLAUDE.md` / `本番反映手順書.md`（個人情報シートのURL記載）/ `LP作成リファレンス.md` /
`gas-recorder/` 13ファイル / `scripts/` 40ファイル / `deploy/` / `v2-deploy/` が
本番URLで誰でも取得できる。認証情報のハードコードはゼロ（GASは全て PropertiesService 経由）。

除外リストを足す実装は `wip/deploy-hygiene`（ローカル）とパッチに退避済み。ただし
**rsync の `--delete` は「除外したファイル」を消さない**（除外＝保護扱い）ので、
除外を足すだけでは既にアップロード済みのものは残り続ける。STGで実証済み（run #493 で
「CLAUDE.md が HTTP 200」を検出して失敗）。`--delete-excluded` は `/thanks/` 等
サーバ側に存在させたままの運用のものまで消すため使えない。

## 今日踏んだ運用の教訓

- **パイプの後ろの `$?` はパイプ最後のコマンドの終了コード**。`release-pre-check.sh` を
  `| tail` に通して `$?` を読み、本番HTTP検証（実際は egress 遮断で exit 56）を
  「通った」と誤報告した。終了コードは直接受けて確認する。
- **YAMLのblock scalar内で、行継続 `\` の途中にコメントを置いてはいけない**。bash は
  行を連結してからトークン化するので `#` 以降が全部消え、rsync の除外指定と
  転送元・転送先ごと落ちる（デプロイが壊れる書き方だった）。
- **E2Eの結果は実行途中のログで判断しない**。`--- N/M passed ---` の最終行を見る。
  途中経過を見て「失敗0」と報告し、実際は14件失敗していた。
