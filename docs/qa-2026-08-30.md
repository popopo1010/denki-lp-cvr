# QA横断チェックと課題台帳（2026-08-30）

全角度QA（静的12種 / E2E 62+17 / UI/UX監査 通常・アプリ内 / XSS / 重複ID /
生成器ドリフト / デプロイ実績）の結果と、未解決課題の棚卸し。

## 今日までに解消した課題

| 課題 | 対処 |
|---|---|
| 入力中に画面がガクガク揺れる（実測23回） | scrollBy を instant 化＋沈静後1回補正。実測6回に |
| ステップ間で余白が52px跳ねる | 全ステップ統一 → オーナー指摘で64pxへ減 → iOS限定化。Android LINE は 146→94px |
| エラー表示のたびにレイアウトが+40px動く | エラー欄を絶対配置に（増分0px）。dk_lp 3本への適用漏れも回収済み |
| 入力中ずっと赤帯が残る | 即時反映化。CTA無効中は必ず理由を出す形に確定 |
| CTA無効なのに理由が出ない（自ら混入→QAで自己検出） | 即日修正済み（252c326） |
| iPhone SE(540px)で step01 のCTAが画面外 | 短画面(≦620px)限定で詰め。denkikouji +99→+37px |
| STG検証が新規LPの未配信を見逃す | 全LPのHTTP応答一覧＋失敗時サーバls を deploy.yml に追加 |
| 生成器が対応職種を「施工管理」へ戻す | variant の label を引くよう修正 |
| FAQ本文と構造化データのズレ | check-faq-schema.mjs（CI 3箇所）で機械的に阻止 |
| 数値3種（96.4% / 34,513人 / 120万円UP） | オーナー確認済みの事実。CLAUDE.md に記録。全LPで表記統一を確認 |

## XSS・その他の確認済み角度

- 求人カード描画（lp-job-cards.js）に悪意JSONを注入 → ペイロード実行0・タグ注入0。
  注記の二重エスケープ（潜在）を修正。
- 重複IDなし（「id="false"」は検査側正規表現の誤検知。`aria-invalid="false"` の部分一致）。
- 代理店共有のPII混入検査・禁止コピー・?v= 整合・参照切れ すべて緑。

## 未解決課題（優先順）

1. **無効表示の「次へ」ボタンがクリックで進む（既存バグ）**
   何も入力せず step06 まで到達でき、電話番号だけのリードが作れる。
   マージ前（a8d58a9）から同挙動。ガードを試作したが step03 で正常入力まで
   詰まったため取り消した（フォームが詰まる方が桁違いに重い）。
   直すには全ステップの is-disable 付与条件の棚卸しが必要。**別作業として着手判断待ち**。
2. **求人カードの本番展開は実求人データ待ち**
   denkikouji-trust（noindex）で仮データ表示までは検証済み。
   assets/data/lp-job-cards-denki.json を実データに差し替え is_sample:false に
   するだけでLP/JSは無改修。**データの出所（シート/API/CSV）待ち**。
3. **会社所在地・代表者が未確認**
   運営者情報ブロックには出していない。確認が取れたら Organization の
   構造化データと表示に追加。
4. **app-v2系のエラー挙動が非統一**
   列挙型（touched無し）のため即時反映化は保留。実機で不満が出たら着手。
5. **sekoukanri step01 は SE(540px) で +116px はみ出しが残る**
   選択肢12個×48px(タップ下限)で幾何学的に収まらない。自動送りがあるため許容中。

## 運用の教訓（今日踏んだもの）

- **main→staging の順で連続pushすると main のデプロイが消える**：
  concurrency グループ（deploy-xserver）内で「待機中」のランは、後から来た
  ランに置き換えられる（GitHubの仕様。cancel-in-progress:false でも起きる）。
  e99147e / d058253 の main デプロイが2連続でこれに食われ、staging だけ
  進んで本番が古いままになった。**mainへ出すときは main を単独で先に push し、
  ランが走り出してから staging を push する**。workflow_dispatch と re-run は
  この統合トークンでは 403 で使えない。
- ローカルの `?v=` 参照は `app.js?v20260829c` 形式（`=` なし）が主流。
  `=` 付きを仮定した一括置換は空振りする。必ず実参照を grep してから置換する。
