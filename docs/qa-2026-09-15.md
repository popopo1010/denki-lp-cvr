# 全体最適化・軽量化とQA（2026-09-15）

オーナー依頼3件（①プラポリ導線の会社名ズレ ②主力LPに「エリアから探す」 ③会社情報に電話番号）を
`claude/stoic-brown-5is8nx` に積んだうえで、「軽量化できるところは軽量化して QA まで」を実施した記録。
**やって効いたこと・効かなかったこと・残したもの**を残す（同じ検討を繰り返さないため）。

現在地: **①作業ブランチに push 済み**。PR未作成・STG未反映・**本番はまだ旧版のまま**。
表示に関わる変更（エリアボタン・theme-lp.css の適用範囲拡大）なので、
`git push -f origin claude/stoic-brown-5is8nx:staging` → STGスマホ実機 → PR → main の順。

## 1. 依頼3件（このブランチの先行コミット）

| # | 内容 | コミット |
|---|---|---|
| ① | LPの同意文リンク先を WPルート `/privacypolicy`（別会社名の固定ページ）から、リポジトリ内の XCHANGE株式会社版 `/denki-lp-cvr/privacypolicy/` へ（126ファイル・224箇所）。`steps-lazy.html` 12本の `?v=` を bump | `1e1842f` |
| ② | 会社情報の直下に「エリアから探す」（8ボタン）。タップでFVの選択肢を実クリック→フォームへ、step04 の都道府県セレクトで押したエリアを先頭 optgroup に。`assets/js/lp-area-nav.js`・各 `index.html` の `<style id="lp-area-css">` | `a5b6e86` |
| ③ | 会社情報に電話番号 03-6693-5450（#127 でオーナー確認済みの番号） | `a5b6e86` |

追補（同日・#141 マージ後）: **利用規約も `terms/index.html` を新設して付け替え**（オーナー判断）。`href="/terms"` 229箇所＋絶対URL 3箇所 → `https://denkilp.builders-job.com/denki-lp-cvr/terms/`。`steps-lazy.html` 12本の `?v=` を `v20260915b`。規約本文は起草したもの＝**要確認**。番人: `check-lp-guard`（root パターンに terms）・`check-minify-coverage`（法務ページとして必須）・`probe-status`（到達確認）。

追補2（軽量化・重複チェック、オーナー依頼）:
- **法務ページ3本（terms / privacypolicy / WPLP/privacypolicy）を `theme-snapshot.css`（gz 7.9KB）→ `theme-lp.css`（3.5KB）へ**。ヘッダー/フッターの規則しか使わないので、
  全要素（::before/::after 含む）の computedStyle と矩形を 390/1280px で突き合わせて **6/6 一致**。`theme-lp.css` の再生成に差分なし（`?v` 据え置き）。
  `check-form-invariants` の監視リストは 15本→18本。terms は minify 後 3.5KB gz（生 3.8KB）。
- **重複チェック**: `WPLP/privacypolicy/index.html` は root 版と CSS パス以外バイト同一（`自前LP/privacypolicy` は自前 `style.css` を読む別物）。
  参照は `WPLP/index.html`（プレビュー一覧・生成物）だけで、LP の同意文はすべて root 版の絶対URLを指す。削除は `generate-lp-index.js` /
  `check-lp-guard` の WPLP パターン / deploy の minify リストまで動くため今回は据え置き（3本を直すときは #125/#127 と同様に全数 grep で揃える）。
  terms 内の id 重複なし。inline CSS は PP と同一系統（`ol` の1行だけ追加）で、外部ファイル化すると往復1回増えるため inline のまま。

追補3（#142 マージ後・「すすめてください」）:
- **残り46本（フォームLP 39本＋service 5本＋nenshu thanks 2本）も `theme-lp.css` へ統一 → theme-snapshot.css 直読み 0本**。`theme-lp.css` は再生成しても差分なし（全HTML/JSの和集合から生成しているため）。
  検証: フォームLP 39本は `visual-diff-theme-lp.mjs`（全ステップ・全状態 × 390/1280px）、静的7本は同等の全要素 computedStyle 比較。結果は下の QA 表。
  静的7本の結果: service 5本は一致、nenshu-shindan(-v2)/thanks 2本は `.t-hero__chk` の `t-pop`・`.t-step__n` の `t-glow` の**アニメーション位相差**だけが出た
  （transform / box-shadow の小数第3位）。`animation:none` を注入して比べ直すと **4/4 一致**＝CSS 差ではない。**計測時はアニメーションを止める**のを標準にする。
  番人は「主力N本のリスト」から **「theme-snapshot.css を直読みするHTMLが無い」** に置き換えた（リストを増やし続ける運用をやめる）。
- **重複の解消**: `WPLP/privacypolicy/index.html`（root 版と同一）を削除。`check-lp-guard` の WPLP パターン・deploy の minify リストから外し、
  `generate-lp-index.js` で一覧を再生成（denkikouji-nd と terms のカードが未反映だったのも同時に反映）。LP からの参照は元々ゼロ。

## 2. 軽量化（本番相当 minify+gzip で計測）

deploy.yml の「Minify JS/CSS/HTML」をそのままローカルで流し（esbuild 0.23.1 / html-minifier-terser 7.2.0）、
初回表示で取得するローカル資産の gzip 合計を実ブラウザ（iPhone 13 相当・load＋3秒アイドル）で数えた。

### 2a. 見つかった取りこぼし（minify 対象外のまま配信されていたもの）

| 対象 | 状態 | 影響 |
|---|---|---|
| `assets/js/lp-area-nav.js`（今回の新規） | deploy の JS リストに無い | 生 2.6KB → minify 1.3KB（gzip） |
| `steps-lazy.html` 13本 | 全LPが load 後に必ず fetch する断片。minify 対象外 | 1本 3.5KB → 3.3KB（gzip）。raw は 14KB → 12.6KB |
| `denkikouji-trust` / `denkikouji-nd` / `dk_lp/*` / `service/*` / `privacypolicy` ×3 | HTML が生のまま | trust 9.3→8.0KB、nd 8.9→7.6KB、dk_lp 10.2→8.6KB（gzip） |

**原因は「minify はファイルの明示リスト」**。2026-08-25 にも同じ理由で広告着地ファミリーが漏れていた。
人がリストを覚えている限り漏れるので、`scripts/check-minify-coverage.mjs` を新設して ci / deploy / release-pre-check の3箇所に入れた
（配信HTMLから参照される JS/CSS・フォームLPの index.html・全 steps-lazy.html・privacypolicy が deploy.yml のリストに載っていること）。

### 2b. theme-lp.css の適用範囲を広げた

2026-09-04 の `theme-lp.css`（テーマCSSからLPで使う規則だけを残した生成物）は主力6本だけが読んでいて、
**広告着地の `meta-lp/` 7本と denkikouji-trust は重い `theme-snapshot.css` 直読みのまま**だった。

| | gzip | minify後 |
|---|---:|---:|
| `theme-snapshot.css` | 7.9KB | 37.5KB |
| `theme-lp.css` | 3.5KB | 14.1KB |

8本を切り替え（レンダーブロックCSSが1本あたり **−4.4KB gzip**、パース対象 −23KB）。
`theme-lp.css` は全LPの HTML/JS に現れる規則の和集合から生成しているので、内容は変えていない（`?v20260904a` のまま）。
`check-form-invariants` の「theme-lp.css を読んでいる」監視リストを 6本 → 15本 に拡大。

残り **56本**（v2・v3〜v6・nenshu-shindan・WPLP・自前LP・service・privacypolicy 等）は `theme-snapshot.css` 直読みのまま。
切り替えは機械的にできるが、1本ずつ `visual-diff-theme-lp.mjs --lp` で computedStyle 一致を見てから（1本 3〜4分）。

### 2c. 初回表示の転送量（gzip 合計・ローカル資産のみ）

| LP | main（本番が今配っているもの） | 今回 | 差 | 内訳 |
|---|---:|---:|---:|---|
| `/denkikouji/` | 55.6KB | 57.2KB | +1.6KB | エリアから探す（JS 1.3KB＋HTML/CSS 0.6KB）− steps-lazy minify 0.2KB |
| `/sekoukanri/` | 57.6KB | 59.3KB | +1.7KB | 同上 |
| `/denkisekou/` | 57.5KB | 59.1KB | +1.6KB | 同上 |
| `/denkikouji-trust/` | 62.0KB | 60.5KB → **56.1KB** | **−5.9KB** | HTML minify −1.3KB、theme-lp.css −4.4KB |
| `/denkikouji-nd/` | 65.7KB | 64.1KB | −1.6KB | HTML/steps-lazy minify |
| `/meta-lp/*/`（7本） | — | — | **−4.4KB／本** | theme-lp.css（HTMLは 2026-08-25 から minify 済み） |

主力3本は「エリアから探す」の分だけ増える（+1.6KB・初期画面の外・LCP/CLS に無関係）。
その分は steps-lazy の minify で 0.2KB 戻し、それ以上は削らない（ボタン8個＋並べ替えJSの最小構成）。

### 2d. やらなかったこと（判断メモ）

- `lp-area-nav.js` を `lp-job-cards.js` に併合: 1リクエスト減るが、求人カードは trust でも読む（エリアは無い）ため
  「JSONを差し替えるだけ」の純度が落ちる。gzip 1.3KB の defer スクリプトなので据え置き。
- エリアのCSSを `cvr-boost-*.css` へ: 共有CSSの `?v=` を 14本分上げる必要がある。各 `index.html` の
  `<style id="lp-area-css">`（gzip 0.6KB）に置き、生成スクリプトが variant ごと外せる形にした。
- 残り56本の theme-lp.css 化: 効果は明確（−4.4KB/本）だが、visual-diff を 56本回すのに 3時間強。広告着地でないLPが大半なので今回は見送り、
  上の「残り」に手順を残した。

## 3. QA結果

| チェック | 結果 |
|---|---|
| 静的チェック18本（workflow-shell / banned-copy / kuma-anchor / lazy-steps / local-refs / form-invariants / input-attrs / faq-schema / asset-versions / lp-guard / denkikouji-release / thanks-v2-release / lp-bridge-release / agency-share / zoho-field-limits / gas-row-integrity / fv-images / **minify-coverage（新設）**） | ✓ すべて通過（form-invariants **139/139**） |
| 生成物ドリフト（ci.yml と同じ生成スクリプト一式を流して差分ゼロ） | ✓ |
| **e2e-lp-flow-local（実ブラウザ・主力3本）** | ✓ **61/61**（新設 `runAreaNav` 含む） |
| **e2e-lp-flow-local を「minify 済みコピー」で（本番相当の出力・9本: denkikouji / sekoukanri / denkisekou / sekoukanri-kentiku / -doboku / denkikouji-trust / denkikouji-nd / dk_lp ×2）** | ✓ **169/169**（minify 後の steps-lazy.html 断片・HTML でも自己修復／遅延ステップ復旧／キーボードナッジ／送信到達まで通る） |
| **e2e-lp-flow-local（`/meta-lp/denkikouji/`・theme-lp.css 化後）** | ✓ **20/20** |
| **visual-diff-theme-lp（主力3本）** | ✓ computedStyle 一致 **108/108** |
| **visual-diff-theme-lp（theme-lp.css へ切り替えた8本 × 390/1280px × 全ステップ・全状態）** | ✓ computedStyle 一致 **288/288**（再試行 0 回） |
| **audit-mobile-ux（3LP × 6機種・横スクロール/タップ44px/入力欄16px/FV内CTA）** | ✓ 問題なし |
| スクショ確認（iPhone 13 相当） | エリアブロックの見た目・タップ→フォーム→step04 の先頭 optgroup・会社情報の電話行 |

## 4. 失敗メモ（つまずいたこと）

- **`npm i --no-save` は前回の `--no-save` を消す**: purgecss を入れると playwright が消え、playwright を入れると purgecss が消える。
  両方使うときは1コマンドで同時に入れる（`npm i --no-save playwright@1.56.1 purgecss@7.0.2`）。E2E 実行中に消すと途中で落ちる可能性がある。
- **`pkill -f "<文字列>"` は自分のシェルも殺す**: コマンド文字列に同じ文字列が含まれるため。ポートから PID を引いて `kill` する。
- **生成スクリプトでブロックを外すときの改行**: `\n<!-- … -->…</section>\n` を `"\n"` に置換すると空行が1行増えて生成物ドリフトになる。`""` に置換する。
- **サンドボックスから `denkilp.builders-job.com` / `xchange-inc.com` は到達不可**（egress 遮断）。本番/HPの表示は GitHub ランナー（probe-status）かオーナーの目視で確認する。

## 5. 本番反映前の確認（STGスマホ実機）

1. 同意文「プライバシーポリシー」のリンク先が XCHANGE株式会社版（会社名・所在地・代表者）
2. 「エリアから探す」の見た目（テーマの `button` 装飾が被らないか・2列に収まるか）→ タップでフォームへ → step04 のセレクト先頭に押したエリア → クマがCTAへ移動
3. 会社情報の「電話 03-6693-5450」が HP と一致
4. `meta-lp/` 7本と `denkikouji-trust` の FV・ステップの見た目（theme-lp.css 化）
5. Deploy ログの「圧縮・キャッシュヘッダ」で `steps-lazy.html` が minify＋gzip で配られていること

## 追補4（2026-09-16）: denkikouji-v2 にも「エリアから探す」＋電話番号

オーナー依頼「`/denkikouji-v2/` にも反映してほしい。メインの3本に加えてほしい」。

- `denkikouji-v2/index.html` に主力3本と同じ4点を追加: `lp-area-nav.js?v20260915a` の読込・`<style id="lp-area-css">`（denkikouji と同一）・`.cvr-about` 直下の `<section class="lp-area" data-lp-area-nav>`・会社情報の「電話 03-6693-5450」。
- v2 は `#pref` が index.html 内にあり（遅延注入なし）、`app-v2.js` の `initPrefSelect` が初期化時に optgroup で埋める。`lp-area-nav.js` は「あれば即並べ替え・無ければ MutationObserver で待つ」なのでそのまま動く。FV の `data-value="今は情報収集したい"` も同じ。
- `theme-lp.css` は再生成しても差分なし（規則は denkikouji の和集合に含まれていた）。`?v=` 据え置き。
- 求人カード（`lp-job-cards.js`）は今回入れていない（依頼はエリア導線）。
- WPLP/denkikouji-v2・自前LP/denkikouji-v2 は別ファイルで未反映（依頼URLは `/denki-lp-cvr/denkikouji-v2/` のみ）。
- **失敗メモ**: denkikouji の `<section class="lp-area">` を「直前2行＋section」でコピーしたところ、HTMLコメントが3行あったため先頭行（`<!--`）が欠け、コメント本文「都道府県ステップ(step04)で… -->」が会社情報の下に**そのまま表示**された（ローカルの 390px スクショで発見・修正）。静的チェックは全部通っていた＝「コメントの開閉が揃っているか」は誰も見ていなかった。`check-local-refs.mjs` に `<!--` と `-->` の個数一致を追加（負のテスト: 先頭行を消すと落ちることを確認）。
- 検証: 静的チェック全通過・`theme-lp.css` 差分なし・実ブラウザで「関東」タップ → `body.lp-form-step`・`.lp-area` 非表示・step01 表示・`#pref` 先頭 optgroup「関東（選択したエリア）」・options 48（47+placeholder）・`lp_area_click` 計測・pageerror 0。E2E（`--lp /denkikouji-v2/`）の結果は下記。
- **e2e-lp-flow-local（`--lp /denkikouji-v2/`・実ブラウザ）: 21/21 通過**（`runAreaNav`: 先頭optgroup=関東（選択したエリア）／先頭=東京都／件数=48／計測=true）。

## 6. このセッション（2026-09-15〜16）で踏んだエラー全件と、仕組みへの反映

| # | 何が起きたか | 原因 | 直し方 | 仕組みへの反映 |
|---|---|---|---|---|
| 1 | サンドボックスに `rsync` が無く minify 済みツリーを作れない | 環境 | `git ls-files -z \| tar` でコピー | メモのみ |
| 2 | `pkill -f "http.server 8765"` で自分のシェルが死ぬ（exit 144） | コマンド文字列が自分にも一致 | `ss -ltnp` で PID を引いて `kill` | メモのみ |
| 3 | `npm i --no-save` で playwright / purgecss が交互に消える | `--no-save` は前回の no-save を prune する | `npm i --no-save playwright@1.56.1 purgecss@7.0.2` を1コマンドで | メモのみ |
| 4 | 生成物ドリフト（variant に空行が1行増える） | `sub("\n")` で置換 | `sub("")` | ci の生成物ドリフト検査が捕まえる（既存） |
| 5 | CI E2E flake: 氏名/生まれ年エラー帯「非表示」（select 年 LP） | 帯は blur の `setTimeout(0)`＋ナッジ後に出る | `waitErrShown`（最長2s ポーリング）を移植 | `e2e-lp-flow-local.mjs` |
| 6 | CI E2E flake: `/denkisekou/` で入力中判定が空振り | `page.click` がラベル中央に当たらず activeElement が移らない | `tap()` がクリック後にフォーカスを確認し無ければ `page.focus` | `e2e-lp-flow-local.mjs` |
| 7 | probe-status が全 000 | ランナー側の一時的なネットワーク障害 | 次の push で緑。PR にコメント | 判断基準をメモ（全 000 ＝本番障害ではない） |
| 8 | `cherry-pick` が CLAUDE.md で衝突 | 同じ節を両側で編集 | abort → `git show <sha> -- scripts/e2e… \| git apply --3way` で必要 hunk だけ | メモのみ |
| 9 | nenshu thanks の computedStyle 差分 | `t-pop` / `t-glow` のアニメーション位相 | `animation:none` で 4/4 一致 | CLAUDE.md の visual-diff 行に「位相差は欠落ではない」 |
| 10 | visual-diff 18件 DIFF（`/dk_lp/denkikouji/`） | クマ `.js-fixed-icon` の inline transform を `startBounce()` が JS で書き続ける（サンプリング差） | 署名から除外 | `visual-diff-theme-lp.mjs`（クマの transform を dump しない） |
| 11 | scratchpad の `.mjs` で `import "playwright"` が解決できない | ESM は呼び出し元ディレクトリから `node_modules` を辿る | 絶対パス `…/node_modules/playwright/index.mjs` | メモのみ |
| 12 | 複数行HTMLコメントの先頭行が欠け、本文が denkikouji-v2 に露出 | ブロックを「直前2行＋本体」でコピー（コメントは3行） | 先頭行を復元 | `check-local-refs.mjs` がコメントの開閉数一致を検査（負のテスト済み） |
| 13 | `release-pre-check.sh` 6/6 本番HTTP が exit 56 で無言終了 | `verify-production-release.sh` が `set -e`、サンドボックスから本番へ到達不可（proxy 403） | 到達性を先に確認して理由を出す。`PRECHECK_SKIP_HTTP=1` で明示スキップ | `release-pre-check.sh` |
| 14 | 「STGで確認して」と案内した時点で STG デプロイ #551 が未完了（オーナー「まだ入ってない」） | PR 作成と同時に案内した（2026-09-04① の再発） | 完了後に再案内 | CLAUDE.md 失敗メモ①（Verify staging deployment success を見てから URL を渡す） |
| 15 | E2E をコメント露出の修正前ファイルで回していた | 修正と E2E 起動の順序 | 修正後に再実行（21/21） | CLAUDE.md 失敗メモ⑥ |

- 仕組み側の変更はこの節と同じコミットで入れた（`visual-diff-theme-lp.mjs` / `release-pre-check.sh` / `check-local-refs.mjs` / CLAUDE.md）。
- 反映確認: `node --check` と `check-workflow-shell.py`（`scripts/*.sh`）通過、`release-pre-check.sh` の 6/6 がサンドボックスで理由付きの exit 2 になること、`visual-diff-theme-lp.mjs --lp /dk_lp/denkikouji/` でクマ由来の DIFF が消えること（下記に結果）。
- **結果**: `visual-diff-theme-lp.mjs --lp /dk_lp/denkikouji/`（クマの transform 除外後）= computedStyle 一致 **28/28**（再試行 0 回・DIFF 0）。除外前は同じLPで DIFF 18 件（すべてクマの inline transform）だった。
