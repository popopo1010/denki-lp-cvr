# 軽量化・高速化（実装済み一覧）

## サンクス `thanks-v2`（2026-06-04 リファクタ）

| 施策 | 効果 |
|------|------|
| **JS bundle** `thanks-v2-deferred.js` | gtm + line-beacon + testimonials + mobile-ux を1本化（HTTP −3） |
| **`thanks-v2-shared.js`** | GAS URL / session / dataLayer / escape 共通化 |
| **ストーリー遅延取得** | `thanks-testimonial-stories.json` preload 廃止（初回展開時のみ fetch、~9KB 節約） |
| **CSS 統合** `thanks-page.css` v20 | `thanks-calendar.css` を結合（HTTP −1） |
| **都道府県→地域マップ** | JS 1.5KB → `thanks-job-previews.json` へ移動 |
| Optimistic 予約 UI | 「予約中...」待ち解消 |
| GAS prewarm + staff_id 高速パス | 予約 API 短縮 |

## サンクス `thanks-v2`（従来）

| 施策 | 効果 |
|------|------|
| 口コミ6件 + 折りたたみ | SP初期表示3-4行 + 大きい「全文を読む」CTA（`thanks-page.css` v12） |
| イラストアバター + 資格マッチ並べ替え | `thanks-testimonials.js` v2・`_license` |
| 予約 bootstrap v12 | 静的JSONのみ表示（GAS JSONP裏取りオフ）+ LSキャッシュ30分 |
| LandingHub 削除 | 外部JS 2本削減 |
| WP `style.css`（52KB）削除 → `thanks-page.css` | CSS 大幅削減 |
| `cvr-boost-v2.css`（26KB）削除 | 同上 |
| GTM 遅延読み込み（操作 or 6秒） | 初回描画をブロックしない |
| `thanks-booking-bootstrap.js` 1本化 | HTTP 往復 −1 |
| `booking-slots.json` preload | 枠表示を前倒し |
| deploy 時 minify | 転送量削減 |

## ビルド

```bash
node scripts/build-thanks-v2-deferred.mjs   # 下部 defer JS を再生成
node scripts/check-thanks-v2-release.mjs    # 静的チェック
```

編集対象: `thanks-gtm.js` 等のパーツファイル → build → deploy minify

## 空き枠

| 施策 | 内容 |
|------|------|
| 静的 JSON | CDN から即表示 |
| GAS CacheService | 3分キャッシュ |
| 5分 sync workflow | JSON 更新 |
| LP 送信時＋電話入力(step06)でプリウォーム | サンクス到達前に枠取得 |
| localStorage 30分キャッシュ | セッション切れても即表示 |
| サンクスで予約JSをカレンダー直後に実行 | defer待ちを回避 |
| スケルトン即表示 | 読み込み中の空白を防止 |
| サンクス遷移 600ms | 1秒→短縮 |
| 取得日数 5日 | GAS 負荷軽減 |

## LP（`*-v2`）

| 施策 | 内容 |
|------|------|
| WP CSS 非同期 | `media="print" onload` |
| Critical CSS インライン | FV 即描画 |
| `app-v2.js` defer | HTML パース優先 |
| GTM lazy | 操作後読込 |
| サンクス遷移 1.5秒→1秒 | 待ち短縮 |

## deploy（`.github/workflows/deploy.yml`）

- `app-v2.js` / 予約JS / GTM / 主要 CSS を esbuild minify
- `thanks-v2` と全 `*-v2/index.html` を HTML minify

## 手動（未反映だと遅いまま）

1. GAS: `clasp push` + `warmBookingSlotsCache` トリガー 5分
2. `sync-booking-slots` workflow が動いているか GitHub Actions で確認

## 計測

Chrome DevTools → Performance / Network:

- `thanks-v2`: WP CSS が **無い**こと
- `booking-slots.json`: **200** かつ TTFB 短いこと
- LCP: ヒーロー＋カレンダー枠が 2.5秒以内（目安）

## 2026-07-02 追加

| 施策 | 内容 |
|------|------|
| `assets/.htaccess` 長期キャッシュ | css/js/画像に `max-age=31536000, immutable`（?v= バスト運用が前提）。JSONは `no-cache`（booking-slots 5分同期のため）。**画像を内容差し替えする時はファイル名を変える** |
| `sekoukanri_hero` 再圧縮 | webp 101KB→68KB / jpg 208KB→132KB（q75/q80・焼き込み文字の可読性確認済み） |
| `sekoukanri_hero-sp.webp` 新設 | 480w・23KB。sekoukanri全LPのモバイルFVを軽量化（imagesrcset） |
| denkikouji-v2 FVバナーに width/height | CLS対策（比率480:288） |
| meta-lp sekoukanri系の未使用preload削除 | 使われないバナーの先読みを解消 |

### 見送り（判断メモ）
- **v1本番へのGTM lazy 展開**: 最大の初期JS削減候補だが、lazyパターンは v2（配信ゼロ）でしか動作実績がなく、
  計測切れ事故（6/4-6/8）の再発リスクがある。**v2のLPABで lead_conversion 実発火を確認してから** v1 に展開する。
- WPテーマ style.css の脱依存: thanks-v2 では実施済みだが、LPはテーマクラス依存が広く工数対効果が合わない。
