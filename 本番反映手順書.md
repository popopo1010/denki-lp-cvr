# 電気工事バンクLP 本番反映手順書

## 対象サイト（広告送客先・canonical）
https://denkilp.builders-job.com/denki-lp-cvr/denkikouji/

※ 旧 WP URL `/denkikouji-kyujin-2/` は deploy 時に 301 で上記へ転送（手動不要）

### denkikouji スタック（2026-06）

| 項目 | パス / 備考 |
|------|-------------|
| LP HTML | `denkikouji/index.html` + `steps-lazy.html` |
| CSS | `assets/css/cvr-boost-denkikouji.css` |
| JS | `assets/js/app.js` |
| Meta LP | `meta-lp/denkikouji/`（再生成: `python3 scripts/generate-meta-lp.py`） |
| Thanks | `thanks-v2/?lp=denkikouji`（旧 `thanks/` はリダイレクト） |
| CV+LINE運用 | `docs/CV-LINE-playbook.md` |

反映後は playbook の **5分チェック**（LP→予約→LINE解放）を実施すること。

## 改善版の確認URL
https://popopo1010.github.io/denki-lp-cvr/denkikouji/

## ステージング（本番同等環境での事前確認・2026-07-08〜）

`staging` ブランチに push すると、本番と**同じ Xserver・同じ minify パイプライン・本物のWPテーマCSS/LandingHub** で `denki-lp-cvr-stg/` に反映される（noindex付き・広告送客先には使わない）。

```
git push -f origin <作業ブランチ>:staging
```

| 環境 | URL |
|------|-----|
| STG denkikouji | https://denkilp.builders-job.com/denki-lp-cvr-stg/denkikouji/ |
| STG sekoukanri | https://denkilp.builders-job.com/denki-lp-cvr-stg/sekoukanri/ |

- **mainマージ前に、スマホ実機（LINE/Instagramアプリ内ブラウザ含む）でSTGを確認する**こと。ローカルではWPテーマCSSが読めず再現しない崩れ（例: 2026-07-08 FV非表示）はSTGでしか事前検知できない。
- GTM/Clarity はSTGでも発火する。確認作業のアクセスが計測に混ざる点は許容（頻度が低いため）。
- WPテーマCSSのスナップショット: `Snapshot WP theme CSS` ワークフロー（手動 or 毎週月曜自動）が本番の style.css を `assets/css/theme-snapshot.css` に取り込み、差分があればPRを作る。ローカル検証はこのスナップショットを使う。

## GitHubリポジトリ
https://github.com/popopo1010/denki-lp-cvr

---

## 広告CR（録画用）専用URL — LPとは別

動画広告の画面録画用ページ。**広告のリンク先（LP送客先）には使わない。**

| 用途 | パス（リポジトリ） | 本番URL（`main` 反映後） |
|------|-------------------|-------------------------|
| CR一覧（社内用） | `ad-cr/` | `https://denkilp.builders-job.com/denki-lp-cvr/ad-cr/` |
| 施工管理・Meta入稿用 | `ad-cr/sekoukanri/?meta=1` | `https://denkilp.builders-job.com/denki-lp-cvr/ad-cr/sekoukanri/?meta=1` |
| 電気工事士・Meta入稿用 | `ad-cr/denkikouji/?meta=1` | `https://denkilp.builders-job.com/denki-lp-cvr/ad-cr/denkikouji/?meta=1` |
| 安全域ガイド表示 | 末尾に `&guide=1` | 上記URL + `&guide=1` |

| LP種別 | 送客先（広告のリンク先） |
|--------|--------------------------|
| 施工管理（全工種） | `.../sekoukanri/` |
| 建築施工管理 | `.../sekoukanri-kentiku/` |
| 土木施工管理 | `.../sekoukanri-doboku/` |
| 電気施工管理 | `.../sekoukanri-denkisekou/` |
| 電気工事士 | `.../denkikouji/` |
| 年収診断（全工種） | `.../nenshu-shindan/sekoukanri/` |
| 年収診断・建築 | `.../nenshu-shindan/sekoukanri-kentiku/` |
| 年収診断・土木 | `.../nenshu-shindan/sekoukanri-doboku/` |
| 年収診断・電気施工管理 | `.../nenshu-shindan/sekoukanri-denkisekou/` |

### Meta広告送客用ショートLP（実フォーム・`noindex`）

通常LPよりFVを短くし、口コミ・FAQ等を非表示。**広告のリンク先として使う実LP**（`ad-cr/` の録画用ページとは別）。

| 工種・種別 | パス | 本番URL（`main` 反映後） | `__LP_ID`（計測用） |
|-----------|------|-------------------------|---------------------|
| 建築施工管理 | `meta-lp/sekoukanri-kentiku/` | `.../meta-lp/sekoukanri-kentiku/` | `sekoukanri-kentiku-meta` |
| 土木施工管理 | `meta-lp/sekoukanri-doboku/` | `.../meta-lp/sekoukanri-doboku/` | `sekoukanri-doboku-meta` |
| 電気施工管理 | `meta-lp/sekoukanri-denkisekou/` | `.../meta-lp/sekoukanri-denkisekou/` | `sekoukanri-denkisekou-meta` |
| 電気工事士 | `meta-lp/denkikouji/` | `.../meta-lp/denkikouji/` | `denkikouji-meta` |
| 年収診断・建築 | `meta-lp/nenshu-shindan-kentiku/` | `.../meta-lp/nenshu-shindan-kentiku/` | `nenshu-shindan-kentiku-meta` |
| 年収診断・土木 | `meta-lp/nenshu-shindan-doboku/` | `.../meta-lp/nenshu-shindan-doboku/` | `nenshu-shindan-doboku-meta` |
| 年収診断・電気施工 | `meta-lp/nenshu-shindan-denkisekou/` | `.../meta-lp/nenshu-shindan-denkisekou/` | `nenshu-shindan-denkisekou-meta` |

- 再生成: `python3 scripts/generate-meta-lp.py`（元LP更新後に実行）
- GTM: 新URL用のページビュー／コンバージョン条件を管理画面で追加すること
- canonical は通常LPを指す（検索流入は通常LPへ）

- CR配下（`ad-cr/`）は `noindex` 済み。検索・LP導線からは切り離す。
- スマホ確認: 同一Wi‑Fiで `python3 -m http.server 8765 --bind 0.0.0.0` → `http://<MacのIP>:8765/ad-cr/sekoukanri/?meta=1`

---

## ABテスト: v2 改善版URL（現行版と並列で稼働）

現行URLを **無変更** で残したまま、改善版（v2）を別URLで公開してABテスト。

### v2 URL 一覧

| 種別 | 旧URL（無変更） | v2 新URL |
|---|---|---|
| 施工管理（全工種） | `.../sekoukanri/` | `.../sekoukanri-v2/` |
| 建築施工管理 | `.../sekoukanri-kentiku/` | `.../sekoukanri-kentiku-v2/` |
| 土木施工管理 | `.../sekoukanri-doboku/` | `.../sekoukanri-doboku-v2/` |
| 電気施工管理 | `.../sekoukanri-denkisekou/` | `.../sekoukanri-denkisekou-v2/` |
| 電気工事士 | `.../denkikouji/` | `.../denkikouji-v2/` |
| 完了画面 | `.../thanks/` | `.../thanks-v2/` |
| meta-lp（各工種） | `.../meta-lp/<lp>/` | `.../meta-lp-v2/<lp>/` |
| nenshu-shindan（各種） | `.../nenshu-shindan/<lp>/` | `.../nenshu-shindan-v2/<lp>/` |
| WPLPプレビュー | `.../WPLP/<lp>/` | `.../WPLP/<lp>-v2/` |
| 自前LPプレビュー | `.../自前LP/<lp>/` | `.../自前LP/<lp>-v2/` |

### v2 アセット（GitHub Pages 上で並列提供）

| 旧版（現行URL用） | v2（新URL用） |
|---|---|
| `assets/css/cvr-boost.css` | `assets/css/cvr-boost-v2.css` |
| `assets/js/app.js` | `assets/js/app-v2.js` |
| `WPLP/assets/css/cvr-boost.css` | `WPLP/assets/css/cvr-boost-v2.css` |
| `WPLP/assets/js/app.js` | `WPLP/assets/js/app-v2.js` |
| `自前LP/assets/css/style.css` | `自前LP/assets/css/style-v2.css` |
| `自前LP/assets/js/app.js` | `自前LP/assets/js/app-v2.js` |

### `_lp` 識別（スプシ集計用）

v2 LP の HTML 内で `window.__LP_ID="<lp>-v2"` を設定。フォーム送信時、Zapier/GAS スプシの `_lp` 列に `-v2` 付きで記録される。

例: `sekoukanri-v2`, `denkikouji-v2`, `sekoukanri-kentiku-meta-v2`, `nenshu-shindan-sekou-v2`

集計時は `_lp` 列でフィルタ:
- 旧版: `sekoukanri`, `sekoukanri-kentiku`, ...
- 新版: `sekoukanri-v2`, `sekoukanri-kentiku-v2`, ...

### 本番反映手順（WordPress + GTM）

1. **WPページ複製**: 現行ページ（例: `/sekoukanri-kyujin-2/`）を複製し、新slug（`-v2` サフィックス）で公開
2. **HTML差し替え**: `v2-deploy/wp-html/<lp>.html` の中身を WP HTMLエディタで貼り付け
3. **GTMタグ追加**:
   - タグ名: `CVR-Boost v2 — <lp>`
   - 種類: カスタムHTML
   - 内容: `v2-deploy/gtm/<lp>.html` を貼り付け
   - トリガー: Page URL contains `/<新slug>/`
4. **広告振り分け**: Google/Meta のキャンペーンを複製し、リンク先を新URLに変更。予算50/50

### ロールバック（v2のみ無効化）

旧URLは完全に独立して稼働しているため、新URL側だけ止めれば終わり。

1. 広告: 新URL向けキャンペーン/広告セットを停止
2. GTM: `CVR-Boost v2 — *` タグを一時停止
3. WP: 新URLページを非公開

### スマホ確認

```
http://<MacのIP>:8765/sekoukanri-v2/
http://<MacのIP>:8765/denkikouji-v2/
http://<MacのIP>:8765/meta-lp-v2/sekoukanri-kentiku/
```

### v2 関連ファイル

- WP管理画面コピペ用HTML: `v2-deploy/wp-html/<lp>.html`
- GTMカスタムHTMLタグ: `v2-deploy/gtm/<lp>.html`
- 詳細手順: `v2-deploy/README.md`

---

## Claude Codeで作業する場合の進め方

`Mastering Claude Code in 30 minutes` の要点を、本LPの本番反映作業では以下の運用に落とし込む。

### 1. まずコードベースQ&Aから始める
いきなりHTML/CSS/JSを編集せず、Claude Codeに以下を確認させる。

- 対象ページで読み込まれているHTML/CSS/JSの構造
- 既存フォームのステップ遷移、バリデーション、送信処理
- GTM注入とテーマ反映のどちらが今回の目的に合うか
- 過去の変更理由が不明な箇所は、必要に応じてGit履歴も確認

### 2. 編集前に短い計画を出す
本番影響がある変更では、Claude Codeに「編集前に計画を出して確認を待つ」よう指示する。

- 変更目的
- 変更対象ファイル
- 影響範囲
- 確認方法
- ロールバック方法

### 3. 検証できる単位で反復する
Claude Codeに作業させる場合も、確認手段を先に決めてから進める。

- HTML変更後: 表示崩れ、CTA、フォームステップ遷移を確認
- CSS変更後: SP/PC表示、固定CTA、モーダル、FAQ開閉を確認
- JS変更後: 入力補助、離脱防止、カウントアップ、送信前同意文言を確認
- 数値・許可番号・口コミ・会社情報を変更した場合: 社内確認項目に追加

### 4. 共有コンテキストを更新する
LP固有の判断ルールや本番反映で毎回使う手順は、必要に応じて `CLAUDE.md` または本手順書へ追記する。個人の好みや一時的な作業メモは共有ルールに入れない。

---

## 3パターンLPの変更ルール

このリポジトリには、通常LP、`WPLP`、`自前LP` の3パターンがある。どれか1つを変更する場合も、他パターンへの影響確認を必ず行う。

| パターン | 主な用途 | CSS/JS |
|---|---|---|
| 通常LP | GitHub Pages / 本番反映確認用 | `assets/css/cvr-boost.css` / `assets/js/app.js` |
| `WPLP` | WordPressテーマCSS併用版 | `WPLP/assets/css/cvr-boost.css` / `WPLP/assets/js/app.js` |
| `自前LP` | WordPress依存なしの単体版 | `自前LP/assets/css/style.css` / `自前LP/assets/js/app.js` |

変更時の原則:
- HTMLだけを変えた場合も、同じLP種別（`denkikouji` / `sekoukanri` / `thanks` / `privacypolicy`）が他パターンにあるか確認する。
- JSのセレクタ、フォームID、classを変える場合は、3パターンすべてのフォームLPで同じ要素が存在するか確認する。
- `自前LP` では外部WordPressテーマCSSに依存させず、原則 `自前LP/assets/css/style.css` を使う。
- 変更後は以下を実行し、相対リンク切れとフォーム構造の破損がないことを確認する。

```bash
node scripts/check-lp-guard.js
```

---

## 反映方法（2通り）

### 方法A：テーマにCSS/JSファイルを追加（推奨）

#### Step 1: ファイルをサーバーにアップロード
以下の2ファイルをFTP/SSH等でアップロードしてください。

```
wp-content/themes/original-thema/assets/css/cvr-boost.css
wp-content/themes/original-thema/assets/js/app.js
```

ファイルはGitHubリポジトリの以下から取得:
- `assets/css/cvr-boost.css`
- `assets/js/app.js`

#### Step 2: テーマでCSS/JSを読み込む
`functions.php` または該当ページテンプレートの `</head>` 前に追加:

```html
<link rel="stylesheet" href="/wp-content/themes/original-thema/assets/css/cvr-boost.css">
```

`</body>` 前に追加:
```html
<script src="/wp-content/themes/original-thema/assets/js/app.js" defer></script>
```

#### Step 3: 既存の読み込みを削除（軽量化）
以下の外部JSは `app.js` に統合済みのため不要:
- `jquery.min.js` （未使用）
- `gsap.min.js` （CSS transitionに置換済み）
- `main.js` （app.jsに統合済み）

#### Step 4: HTMLの変更
GitHubリポジトリの `denkikouji/index.html` を参考に、
WordPress管理画面で該当ページのHTMLを更新してください。

主な変更箇所:
- フォーム前に「信頼性バー」「社会的証明」「リアルタイム通知」を追加
- 各ステップにマイクロコピーを追加
- フォーム後に「利用者の声」「よくある質問」を追加
- 最終ステップにPP同意テキストを追加
- フローティングCTAを追加
- フッターの許可番号を変更

---

### 方法B：GTMで注入（テーマを触らずに反映）

GTM（GTM-KV525PZ）のカスタムHTMLタグで `cvr-boost.css` と `app.js` を注入。
テーマファイルを一切触らずに反映可能。ロールバックも即座にできる。

#### 手順
1. GTM管理画面にログイン
2. 新しいタグを作成 → カスタムHTML
3. 以下を貼り付け:

```html
<link rel="stylesheet" href="https://popopo1010.github.io/denki-lp-cvr/assets/css/cvr-boost.css">
<script src="https://popopo1010.github.io/denki-lp-cvr/assets/js/app.js" defer></script>
```

4. トリガー: ページURL が `/denkikouji-kyujin-2/` を含む
5. 公開

※GitHub PagesのURLから直接読み込むため、サーバーアップロード不要

---

## フォーム送信データの記録（Zapier + Google Sheets）

### 送信先2系統（並列）

LPフォーム送信時、`assets/js/app.js` の `initZapierMirror()` が以下2つに並列送信する。**Zapierは恒久維持**。

| 送信先 | 用途 | ロールバック |
|---|---|---|
| Zapier Webhook `hooks.zapier.com/hooks/catch/2795777/3sgrmvb/` | 既存運用（メール通知等） | コード内 `ZAPIER_URL` を空文字に |
| GAS Web App `script.google.com/macros/s/AKfycbzC.../exec` | スプシ `LP form recorder` (XCHANGEアカウント) に行追記 | コード内 `GAS_URL` を空文字に |

スプシURL: https://docs.google.com/spreadsheets/d/1JwwkLThWTMMmi9p1CMGK8gAz-I5f9cmueGFFpplZwGc/edit
GAS owner: yuki.shibayama@xchange-inc.com

### 面談予約 → スプシ + Slack

- **既定: 独自予約**（Googleカレンダー + GAS + サンクスUI）→ `gas-recorder/独自予約セットアップ.md`
- 切替: `assets/js/thanks-booking-config.js` の `THANKS_BOOKING_MODE`（`custom` / `timerex`）
- TimeRex利用時: `gas-recorder/TimeRex-Slack連携セットアップ.md`
- GAS: `clasp push` + redeploy + カレンダー権限承認 + `SLACK_WEBHOOK_URL`（任意）

### 記録される項目

`_lp` でLP判別（`sekoukanri` / `denkikouji` / `sekoukanri-doboku` / `sekoukanri-kentiku` / `sekoukanri-denkisekou` / `nenshu-shindan-*` / `*-meta` 等）。フォーム全項目（電話・氏名・生年月日・郵便番号・都道府県・市区町村・資格・経験・転職意欲・利用規約同意）と、メタ情報（受信時刻・送信時刻・URL・referrer・IP・UA）。タイムスタンプは **日本時間（Asia/Tokyo）**。生年月日は `your-birthday` 列に `YYYY-MM-DD` 形式で集約される。

### GAS スクリプトの更新方法

正本: `gas-recorder/コード.js`（clasp 管理下）

| 識別子 | 値 |
|---|---|
| Script ID | `1PLdit_rSh-OaParVho8E-DhBFZz4lqSa7ZF6EFsLBzncs5uERinNFUl2` |
| Web App Deployment ID | `AKfycbzC4fMEbOhaymimRwaLDJ34eKwSRyfYVVRMeNGl_cMjR8p7dC9cVw84YZJUvggkROiKRw` |

**clasp 経由（推奨・最短）:**
```bash
cd gas-recorder
# コードを編集 (gas-recorder/コード.js)
clasp push -f
clasp redeploy AKfycbzC4fMEbOhaymimRwaLDJ34eKwSRyfYVVRMeNGl_cMjR8p7dC9cVw84YZJUvggkROiKRw
```
- URL は変わらないので `app.js` 側は触らない
- 初回は `npm i -g @google/clasp` + `clasp login` （XCHANGEアカウント認証）が必要

**手動コピペ（GAS editor）:**
1. GAS editor を XCHANGEアカウントで開く（プロジェクト名 `LP_form_recorder` / Script ID 上記）
2. `Code.gs` の中身を全削除 → `gas-recorder/コード.js` の中身をコピペ
3. Cmd+S 保存
4. 「デプロイを管理」→ 既存デプロイの ✎ → バージョン: 新バージョン → デプロイ

### 動作確認

```bash
# GET (alive チェック)
curl -sL "https://script.google.com/macros/s/AKfycbzC4fMEbOhaymimRwaLDJ34eKwSRyfYVVRMeNGl_cMjR8p7dC9cVw84YZJUvggkROiKRw/exec"
# → "LP form recorder is alive."

# POST (テストデータ)
curl -sL -X POST "https://script.google.com/macros/s/AKfycbzC.../exec" \
  -H "Content-Type: application/x-www-form-urlencoded; charset=UTF-8" \
  --data-urlencode "_lp=curl-test" --data-urlencode "your-tel=09000000000"
# → スプシ form_submissions シートに行が追加される
```

**注意**: curl で `-L` 付きの場合、リダイレクト末端でHTTP 405や「ページが見つかりません」が返ることがあるが、これは表示の問題でサーバー側のPOSTは正常に処理されている。スプシで実際に行が追加されたかで判定する。

### LP判別IDの一覧 (`window.__LP_ID`)

| LP | __LP_ID |
|---|---|
| `/sekoukanri/` | `sekoukanri` |
| `/denkikouji/` | `denkikouji` |
| `/sekoukanri-doboku/` | `sekoukanri-doboku` |
| `/sekoukanri-kentiku/` | `sekoukanri-kentiku` |
| `/sekoukanri-denkisekou/` | `sekoukanri-denkisekou` |
| `/nenshu-shindan/sekoukanri/` | `nenshu-shindan-sekou` |
| `/nenshu-shindan/denkikouji/` | `nenshu-shindan-denki` |
| `/nenshu-shindan/sekoukanri-doboku/` | `nenshu-shindan-doboku` |
| `/nenshu-shindan/sekoukanri-kentiku/` | `nenshu-shindan-kentiku` |
| `/nenshu-shindan/sekoukanri-denkisekou/` | `nenshu-shindan-denkisekou` |
| `/meta-lp/<工種>/` | `<工種>-meta` |























---

## 変更内容サマリー

### CVR改善要素（追加）
| 要素 | 内容 |
|---|---|
| 信頼性バー | 厚生労働大臣許可 / SSL暗号化 / 完全無料 |
| 社会的証明 | 取扱求人13,829件 / 利用者34,513人 / 満足度94% |
| リアルタイム通知 | 「東京都の方が3分前に登録しました」ローテーション |
| マイクロコピー | 各ステップに「なぜこの情報が必要か」の説明 |
| 利用者の声 | 2件の体験談（年収アップ等） |
| よくある質問 | 3件のFAQ（無料？バレない？等） |
| フローティングCTA | スクロール15%超で画面下部に固定表示 |
| PP同意テキスト | 最終送信ボタン下に表示 |
| 数値カウントアップ | 社会的証明の数値がアニメーション |
| 離脱防止モーダル | フォーム途中で離脱時にポップアップ |

### 軽量化（削除・置換）
| 対象 | 削減量 |


|---|---|
| jQuery | -87KB（未使用のため削除） |
| GSAP | -71KB（CSS transitionに置換） |
| CF7 JS/CSS | -27KB（app.jsに統合） |
| 年月日/都道府県select | -17KB（JS動的生成に変更） |
| **合計** | **約200KB削減** |

### 許可番号変更
```
変更前: 厚生労働大臣 許可番号 ２７−ユー３０３２７５
変更後: 有料職業紹介事業許可番号 13-ユ-316946
```

### プライバシーポリシーページ
`/privacypolicy/` にXCHANGE株式会社版を新規作成済み

---

## 確認事項（社内で確定後に反映）

| 項目 | 現在の値 | 要確認 |
|---|---|---|
| 取扱求人数 | 13,829件 | 最新の実数に更新？ |
| 利用者数 | 34,513人 | 最新の実数に更新？ |
| 満足度 | 94% | エビデンスあり？ |
| 今月の新着 | 287件 | 動的取得 or 手動更新？ |
| 利用者の声 | ダミー2件 | 実際のレビューに差し替え？ |
| PP記載の所在地/代表者 | 仮 | 正式情報に差し替え |

### v3 / v4 リッチ版（`denkikouji-v3` / `denkikouji-v4`）の仮置き数値

参考LP（build-job）に合わせて追加した縦長コンテンツの数値は**すべて仮置き（要確認）**。
配信前に実データへ差し替えること。HTML内に `<!-- 要確認 -->` コメントを付与済み。

| 項目 | 現在の仮値 | 要確認 |
|---|---|---|
| 年収UP実績グラフ | 第二種350→470万(+120) / 第一種420→580万(+160) / 施工管理480→700万(+220) | 実績データに差し替え |
| 非公開求人の給与例 | 330〜900万のレンジ4件 | 実際の取扱求人レンジに差し替え |
| Google評価スコア | ★4.7 | 実スコア・件数の確認（無ければ非表示） |
| 利用者満足度 / 利用者数 | 94% / 34,513人 | 既存LP踏襲（根拠は上表と同様に要確認） |
| 比較表（他社 ◎△） | 定性評価 | 表記の妥当性を社内確認 |

※ CSS: `assets/css/cvr-boost-denkikouji-rich.css`（v3/v4 のみ読込・本番 `denkikouji/` には影響なし）

## デプロイが失敗したとき

- `ssh: connect to host ... Connection timed out` → Xserver側の一時的な接続障害。コード起因ではないので **Actions の Re-run jobs**（または空コミットのmain push）で再実行する。

## Claude Code作業後の最終チェック

- `git diff` で意図しないファイル変更がないか確認
- 表示確認URLと本番対象URLを取り違えていないか確認
- GTM反映の場合、対象URLトリガーが `/denkikouji-kyujin-2/` に限定されているか確認
- テーマ反映の場合、アップロード先と読み込みパスが一致しているか確認
- ロールバック手順（GTMタグ停止、または追加CSS/JS読み込み削除）を関係者に共有
