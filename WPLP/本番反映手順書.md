# 電気工事バンクLP 本番反映手順書

## 対象サイト
https://denkilp.builders-job.com/denkikouji-kyujin-2/

## 改善版の確認URL
https://popopo1010.github.io/denki-lp-cvr/denkikouji/

## GitHubリポジトリ
https://github.com/popopo1010/denki-lp-cvr

---

## 反映方法（2通り）

### 方法A：テーマにCSS/JSファイルを追加（推奨）

#### Step 1: ファイルをサーバーにアップロード
以下の2ファイルをFTP/SSH等でアップロードしてください。

```
wp-content/themes/original-thema/assets/css/cvr-boost.css
wp-content/themes/original-thema/assets/js/app.js
```

ファイルはGitHubリポジトリの以下から取得:
- `assets/css/cvr-boost.css`
- `assets/js/app.js`

#### Step 2: テーマでCSS/JSを読み込む
`functions.php` または該当ページテンプレートの `</head>` 前に追加:

```html
<link rel="stylesheet" href="/wp-content/themes/original-thema/assets/css/cvr-boost.css">
```

`</body>` 前に追加:
```html
<script src="/wp-content/themes/original-thema/assets/js/app.js" defer></script>
```

#### Step 3: 既存の読み込みを削除（軽量化）
以下の外部JSは `app.js` に統合済みのため不要:
- `jquery.min.js` （未使用）
- `gsap.min.js` （CSS transitionに置換済み）
- `main.js` （app.jsに統合済み）

#### Step 4: HTMLの変更
GitHubリポジトリの `denkikouji/index.html` を参考に、
WordPress管理画面で該当ページのHTMLを更新してください。

主な変更箇所:
- フォーム前に「信頼性バー」「社会的証明」「リアルタイム通知」を追加
- 各ステップにマイクロコピーを追加
- フォーム後に「利用者の声」「よくある質問」を追加
- 最終ステップにPP同意テキストを追加
- フローティングCTAを追加
- フッターの許可番号を変更

---

### 方法B：GTMで注入（テーマを触らずに反映）

GTM（GTM-KV525PZ）のカスタムHTMLタグで `cvr-boost.css` と `app.js` を注入。
テーマファイルを一切触らずに反映可能。ロールバックも即座にできる。

#### 手順
1. GTM管理画面にログイン
2. 新しいタグを作成 → カスタムHTML
3. 以下を貼り付け:

```html
<link rel="stylesheet" href="https://popopo1010.github.io/denki-lp-cvr/assets/css/cvr-boost.css">
<script src="https://popopo1010.github.io/denki-lp-cvr/assets/js/app.js" defer></script>
```

4. トリガー: ページURL が `/denkikouji-kyujin-2/` を含む
5. 公開

※GitHub PagesのURLから直接読み込むため、サーバーアップロード不要

---

## 変更内容サマリー

### CVR改善要素（追加）
| 要素 | 内容 |
|---|---|
| 信頼性バー | 厚生労働大臣許可 / SSL暗号化 / 完全無料 |
| 社会的証明 | 取扱求人13,829件 / 利用者34,513人 / 満足度94% |
| リアルタイム通知 | 「東京都の方が3分前に登録しました」ローテーション |
| マイクロコピー | 各ステップに「なぜこの情報が必要か」の説明 |
| 利用者の声 | 2件の体験談（年収アップ等） |
| よくある質問 | 3件のFAQ（無料？バレない？等） |
| フローティングCTA | スクロール15%超で画面下部に固定表示 |
| PP同意テキスト | 最終送信ボタン下に表示 |
| 数値カウントアップ | 社会的証明の数値がアニメーション |
| 離脱防止モーダル | フォーム途中で離脱時にポップアップ |

### 軽量化（削除・置換）
| 対象 | 削減量 |
|---|---|
| jQuery | -87KB（未使用のため削除） |
| GSAP | -71KB（CSS transitionに置換） |
| CF7 JS/CSS | -27KB（app.jsに統合） |
| 年月日/都道府県select | -17KB（JS動的生成に変更） |
| **合計** | **約200KB削減** |

### 許可番号変更
```
変更前: 厚生労働大臣 許可番号 ２７−ユー３０３２７５
変更後: 有料職業紹介事業許可番号 13-ユ-316946
```

### プライバシーポリシーページ
`/privacypolicy/` にXCHANGE株式会社版を新規作成済み

---

## 確認事項（社内で確定後に反映）

| 項目 | 現在の値 | 要確認 |
|---|---|---|
| 取扱求人数 | 13,829件 | 最新の実数に更新？ |
| 利用者数 | 34,513人 | 最新の実数に更新？ |
| 満足度 | 94% | エビデンスあり？ |
| 今月の新着 | 287件 | 動的取得 or 手動更新？ |
| 利用者の声 | ダミー2件 | 実際のレビューに差し替え？ |
| PP記載の所在地/代表者 | 仮 | 正式情報に差し替え |
